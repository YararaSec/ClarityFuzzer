All contracts are from Clarity offcial github.


Install:

```
pip install tree-sitter
pip install tree-sitter-clarity
````

`python3 ClarityTranspiler/ClarityTranspiler.py ClarityTranspiler/contracts/basic_types.clar`
